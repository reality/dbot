{
    "dbType": "redis",
    "dbKeys": [ "polls" ],
    "ignorable": true,
    "dependencies": [ "users", "web", "event" ],
    "schema": {
        "poll": {
            "pkey": "name",
            "fields": [ "description", "owner", "votes", "votees" ]
        }
    }
}
